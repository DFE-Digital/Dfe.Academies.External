@model List<Dfe.Academies.External.Web.ViewModels.ApplicationAuditViewModel>

<h2 class="govuk-heading-l">Recent activity</h2>
<p class="govuk-body">Changes to the application by all contributors will appear here</p>

@if (Model.Count > 0)
{
    <ul class="app-task-list__items govuk-padding-left-override">
        @foreach (var audit in Model)
        {
            <li class="app-task-list__item">
                <span class="app-task-list__task-name"><strong>@audit.When.ToString("dd MMM yyyy")</strong></span>
                <span class="app-task-list__task-name">@audit.What</span>
            </li>
        }
    </ul>
    <a asp-page="" class="govuk-link">See the full change history</a>
} 
